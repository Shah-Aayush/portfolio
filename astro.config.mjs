// @ts-check
import { defineConfig } from 'astro/config';

// Update `site` to your real URL once Cloudflare Pages assigns it
// (e.g. https://aayush-shah.pages.dev) or after attaching a custom domain.
export default defineConfig({
  site: 'https://aayush-shah.pages.dev',
});
